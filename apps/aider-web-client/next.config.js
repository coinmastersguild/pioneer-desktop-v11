/** @type {import('next').NextConfig} */
const nextConfig = {
  // Removed static export options
  // We'll run the Next.js server separately and proxy to it from Aider service
}

module.exports = nextConfig 